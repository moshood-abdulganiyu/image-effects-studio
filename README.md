# image-effects-studio
OpenCV-powered web app to apply five image effects — cartoonify, grayscale, pencil sketch, oil painting, pixel art — on demand. FastAPI backend, vanilla HTML/CSS/JS frontend. Upload once, generate any effect independently, download each result. Deployed on Render (API) + Hugging Face Static Space (UI).
